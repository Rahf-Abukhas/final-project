import { AnotherPipePipe } from './another-pipe.pipe';

describe('AnotherPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AnotherPipePipe();
    expect(pipe).toBeTruthy();
  });
});
